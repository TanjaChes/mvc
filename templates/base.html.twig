<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="{{ asset('img/Turtle.png') }}">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <header class="site-header" style="background-image: url({{ asset('img/OceanPhoto.jpg') }})">
            <span class="site-title">My Symfony Site</span>
        </header>
        <nav class="navbar">
            <ul>
                <li><a href="{{ path('home') }}">Home</a></li>
                <li><a href="{{ path('about') }}">About</a></li>
                <li><a href="{{ path('report') }}">Report</a></li>
                <li><a href="{{ path('lucky') }}">Lucky day</a></li>
                <li><a href="{{ path('quote') }}">Quote</a></li>
            </ul>
        </nav>

        {% block body %}{% endblock %}</main>

        <footer class="footer">
        {% block footer %}
            <p>Denna sidan är Copyright &copy; 2023 av mig.</p>
            <p>Kontakt: chesnokova.sweden@gmail.com</p>
        {% endblock %}</footer>
    </body>
</html>
